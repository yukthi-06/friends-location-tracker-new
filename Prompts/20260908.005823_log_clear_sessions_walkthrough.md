# User Prompt
> the clear session action should be logged in LOG file

# Walkthrough

## Summary of Changes
- **Updated `MainActivity.java`**: Added `AppLogger.log(...)` calls inside the `menuClearSessions` click listener.
  - Logs when the clear session request is successfully dispatched to Cloudflare.
  - Logs a warning when the user attempts to clear sessions but the Cloudflare URL is not configured.
