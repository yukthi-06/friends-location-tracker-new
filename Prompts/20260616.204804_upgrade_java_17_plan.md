# User Prompt
upgrade java to 17

# Goal
Verify and ensure Java compatibility is upgraded to Java 17 in the project settings and configuration.

## Current Status Analysis
We analyzed the files in [Friends_Location_Tracker_APK_GITHUB](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB):
- [app/build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB/app/build.gradle) already has:
  ```groovy
  compileOptions {
      sourceCompatibility JavaVersion.VERSION_17
      targetCompatibility JavaVersion.VERSION_17
  }
  ```
- The CI workflow [.github/workflows/android.yml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB/.github/workflows/android.yml) already configures JDK 17:
  ```yaml
  - name: Set up JDK 17
    uses: actions/setup-java@v4
    with:
      distribution: 'temurin'
      java-version: '17'
  ```
- No other gradle sub-projects or configuration files define or reference older Java versions.

## Proposed Actions
Since the Java 17 configuration is already active in this repository:
1. Confirm with the user that the configuration is correct and no changes are needed.
2. If any compilation warning or issue is observed by the user on their local setup, document recommendations for setting the gradle JVM or Android Studio JDK to Java 17.

## Verification Plan
### Manual Verification
- Verify that Gradle successfully recognizes Java 17 configurations.
