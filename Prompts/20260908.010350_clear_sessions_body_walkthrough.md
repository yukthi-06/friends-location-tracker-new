# User Prompt
> the body for the /clear endpint should be the sessionid

# Walkthrough

## Summary of Changes
- **Updated `MainActivity.java`**: 
  - Calculated the `sessionId` inside the `menuClearSessions` click listener based on the `tracked_friends` list, mirroring how it's done for the delete endpoint.
  - Passed `sessionId` as a parameter to the updated `sendCloudflareClear` method.
  - Altered the payload of the POST request to include the JSON body `{"sessionid": sessionId}` instead of sending an empty JSON object `{}`.
